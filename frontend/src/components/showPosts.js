import React, { Component } from 'react'

class ShowPosts extends Component {
  render () {
    return (
      {this.props.category ? }
    )
  }
}

/*
TODO Make a component that shows posts in one or all categories.
Add access to Redux store here.

*/